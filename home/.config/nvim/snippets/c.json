{
  "include guard": {
    "prefix": "#ifn",
    "body": [
      "#ifndef ${1}",
      "#define ${1}",
      "${2}",
      "#endif /* ${1} */"
    ]
  },
  "typedef struct": {
    "prefix": "typedef",
    "body": [
      "typedef struct ${1} {",
      "  ${2}",
      "} ${1}_t;"
    ]
  }
}
